class Coordinates
{
    constructor(x, y, ycos, xsin)
    {
        this.x = parseFloat(x);
        this.y = parseFloat(y);
        this.ycos = parseFloat(ycos);
        this.xsin = parseFloat(xsin);
    }
}